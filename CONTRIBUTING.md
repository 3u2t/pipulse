# Contributing

Small project, simple rules: keep it understandable, keep it light on the Pi, don't add a dependency when stdlib does the job. Open an issue before big changes. `npm test` in backend + frontend and `go -C agent test ./...` should pass.
